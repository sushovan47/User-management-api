#Application name and version and local running port
spring.application.name=spring-boot-latest-practice
spring.application.version=1.0
server.port=8090

#db credentials
spring.datasource.url=jdbc:mysql://localhost/latest_code_db
spring.datasource.username=root
spring.datasource.password=ENC(UDO+IBkkvsLMtrbApMzEAkmr/dPGjoX7)

# Disable automatic schema updates/drops
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=none
spring.jpa.generate-ddl=false
# Shows the SQL query in the console
spring.jpa.show-sql=true
#Prettify/format the SQL output for better readability
spring.jpa.properties.hibernate.format_sql=true

#spring.sql.init.mode=always
#spring.cache.cache-names=configCache
spring.jackson.deserialization.FAIL_ON_NULL_FOR_PRIMITIVES=false

#Redis credentilas for DEV
spring.data.redis.host=localhost
spring.data.redis.port=6379



